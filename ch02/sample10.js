// 함수의 선언

/* 
함수 선언 시 function을 통해 선언하고, 
() 안에 파라미터를 지정하고, 
 {}문 안에 로직을 작성하고,
return을 통해 반환 값(결과 값)을 저장
*/

// 인자 a와 b의 합을 반환하는 함수
function add(a, b) {
    return a + b;
}

// 1 + 4 출력
console.log(add(1, 4));